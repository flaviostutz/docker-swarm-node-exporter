version: '3.5'

services:

  node-exporter:
    build: .
    ports:
      - 9100:9100
    environment: 
      - NODE_ID=test
    # volumes: 
    #   - /proc:/host/proc:ro
    #   - /sys:/host/sys:ro
    #   - /:/rootfs:ro
    #   - /etc/hostname:/etc/nodename
      
